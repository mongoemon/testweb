# Render.com Blueprint — 3 environments
# Branch → Environment:
#   qa      → QA      (auto-seed, fresh DB per deploy)
#   staging → STAGING (no auto-seed, stable data)
#   main    → PROD    (no auto-seed, production data)

services:

  # ── QA ───────────────────────────────────────────────────────────────────
  - type: web
    name: shoeshub-qa
    env: python
    branch: qa
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENVIRONMENT
        value: qa
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: AUTO_SEED
        value: "true"
      - key: CORS_ORIGINS
        value: "*"

  # ── STAGING ──────────────────────────────────────────────────────────────
  - type: web
    name: shoeshub-staging
    env: python
    branch: staging
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENVIRONMENT
        value: staging
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: AUTO_SEED
        value: "false"
      - key: CORS_ORIGINS
        value: "*"

  # ── PROD ─────────────────────────────────────────────────────────────────
  - type: web
    name: shoeshub-prod
    env: python
    branch: main
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENVIRONMENT
        value: prod
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: AUTO_SEED
        value: "false"
      - key: CORS_ORIGINS
        value: "*"
